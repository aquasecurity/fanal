run:
  timeout: 5m
linters:
  enable: 
    - contextcheck
    - errorlint
    - gci
    - gofmt
    - gofumpt
  disable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - structcheck
    - unused 
linters-settings:
  gci:
    local-prefixes: github.com/aquasecurity/fanal
